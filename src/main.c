#include <stdio.h>

int main(int argc, char **argv)
{
    // parse args

    // traverse directory and save paths to files

    // read files in parallel and search for the pattern using Boyer-Moore's algorithm

    return 0;
}
